# ✨ DarkStream Setup Script ✨

> **Автоматическая настройка сервера с помощью скрипта от ScumDev**

## ⚡️ Описание
Этот скрипт автоматизирует настройку сервера для создания локальной сети с DHCP, WireGuard VPN и NAT. Сервер будет пропускать трафик от устройств в локальной сети, перенаправлять его через WireGuard и отдавать с другим IP-адресом, раздаваемым через DHCP.

---

## ⚙️ Установленные пакеты
- `htop` — Мониторинг процессов.
- `net-tools` — Инструменты для работы с сетью.
- `mtr` — Диагностика сети.
- `network-manager` — Управление сетевыми интерфейсами.
- `isc-dhcp-server` — DHCP-сервер для раздачи IP-адресов.
- `wireguard` — VPN-протокол для защищённого соединения.
- `wireguard-tools` — Инструменты для работы с WireGuard.
- `resolvconf` — Управление DNS.
- `ufw` — Простая настройка брандмауэра.
- `iptables` — Для работы NAT.
- `nano` — Текстовый редактор для работы с конфигурациями.

---

## 🔧 Как использовать

### ✅ Подготовка

1. Убедитесь, что у вас есть сервер с установленной ОС Ubuntu (20.04 или выше).

2. Выполните вход на сервер через SSH:

3. **Подготовьте WireGuard**:
   - Установите WireGuard (если не установлен):

     ```bash
     sudo apt install wireguard -y
     ```

   - ⚠️ **ВАЖНО**: Убедитесь, что у вас есть конфигурационный файл WireGuard (`wg0.conf`) и он находится в папке `/etc/wireguard/`. ⚠️

   - Проверьте права доступа к файлу:

     ```bash
     sudo chmod 600 /etc/wireguard/wg0.conf
     ```

4. **Проверьте название интерфейсов сети**:
   - Для этого выполните команду:

     ```bash
     ifconfig
     ```

   - Убедитесь, что вы знаете названия входного и выходного интерфейсов, например, `enp0s0` и `enp1s0`.
   
5. **Проверьте какая у вас маска подсети**

![networks2](https://github.com/user-attachments/assets/f833b014-34dc-4806-8160-60c0793babf7)

### ⚖️ Шаги настройки

#### 1. Клонируйте репозиторий и запустите скрипт:

```bash
sudo apt install git -y
```

```bash
git clone https://github.com/ScumDev1488/DarkStream.git
cd DarkStream
chmod +x darkserver-setup.sh
sudo ./darkserver-setup.sh
```

#### 2. Следуйте подсказкам скрипта:
- ✍ **Введите название ВХОДНОГО интерфейса** (например, `enp0s0`): Интерфейс, через который сервер подключён к провайдеру.
- ✍ **Введите название ВЫХОДНОГО интерфейса** (например, `enp1s0`): Интерфейс для локальной сети.
- ✍ **Введите подсеть для локальной сети** (например, `5`): Это часть IP-адреса локальной сети (`192.168.5.0`).
- ✍ **Введите статический IP от провайдера** (например, `10.10.10.10`): Ваш внешний IP.
- ✍ **Введите маску подсети от провайдера** (например, `24`).
- ✍ **Введите шлюз от провайдера** (например, `10.10.10.1`).

#### 3. Подождите завершения установки:
Скрипт автоматически выполнит:
- Обновление пакетов.
- Настройку сетевых интерфейсов с помощью `netplan`.
- Настройку DHCP-сервера.
- Настройку WireGuard.
- Настройку NAT для маршрутизации трафика.
- Настройку брандмауэра UFW для пропуска и фильтрации трафика.

---

### 🛠 Использование дополнительных пакетов

#### `htop`
Используйте для мониторинга загрузки сервера, процессов и использования ресурсов:

```bash
htop
```

- Вы увидите интерактивный интерфейс для управления процессами и анализа нагрузки CPU, RAM и дисков.

#### `net-tools`
Пакет предоставляет классические сетевые утилиты, такие как `ifconfig`:

```bash
ifconfig
```

- Показывает состояние сетевых интерфейсов.
- Полезно для диагностики соединений.

#### `mtr`
Диагностика сетевого маршрута и задержек:

```bash
mtr 8.8.8.8
```

- Позволяет проверить маршрут до удалённого сервера и выявить проблемы с сетью.

#### `nano`
Редактирование конфигурационных файлов (например, `wg0.conf`):

```bash
sudo nano /etc/wireguard/wg0.conf
```

- Удобный текстовый редактор для правки файлов в терминале.

#### `ufw`
Управление брандмауэром сервера:
- Проверка текущего состояния:

  ```bash
  sudo ufw status
  ```

- Открытие порта (например, 80):

  ```bash
  sudo ufw allow 80
  ```

#### `iptables`
Просмотр и настройка правил NAT:

```bash
sudo iptables -t nat -L
```

- Полезно для проверки корректной настройки маршрутизации.

---

### 🔄 Замена конфигурационного файла WireGuard
Если ваш текущий конфигурационный файл WireGuard (`wg0.conf`) перестал работать, выполните следующие действия:

1. **Путь к файлу для замены:**

   `/etc/wireguard/wg0.conf`

2. **После замены файла перезапустите WireGuard:**

   ```bash
   sudo systemctl restart wg-quick@wg0
   ```

3. **Проверьте состояние подключения:**

   ```bash
   sudo wg show
   ```

---

### 🔒 Поток трафика через сервер
После настройки ваш сервер будет выполнять следующие функции:
1. Принимать трафик от устройств в локальной сети через DHCP.
2. Пропускать трафик через WireGuard для обеспечения безопасности и изменения исходящего IP-адреса.
3. Отправлять обработанный трафик обратно в локальную сеть через NAT.

---

## 🌐 Доступ к серверу
После завершения настройки:
- SSH доступ будет разрешён.
- Устройства в локальной сети будут автоматически получать IP-адреса от DHCP-сервера.
- Весь трафик будет маршрутизироваться через WireGuard.

---

## ⚒️ Полезные команды

### Перезапуск DHCP-сервера

```bash
sudo systemctl restart isc-dhcp-server
```

### Проверка состояния WireGuard

```bash
sudo wg show
```

### Проверка NAT и маршрутизации

```bash
sudo iptables -t nat -L
```

### Проверка UFW правил

```bash
sudo ufw status
```

### Просмотр сетевых интерфейсов

```bash
ip a
```

---

## 🚀 Перезагрузка сервера
После завершения настройки рекомендуется перезагрузить сервер, чтобы применить все изменения:

```bash
sudo reboot
```

---

## 🙌 Авторы
Разработано для сохранения нервных клеток by ScumDev для быстрого и удобного развертывания сетевых серверов с маршрутизацией и изменением IP.

Если у вас есть предложения или вопросы, создайте Issue в [репозитории](https://github.com/ScumDev1488/DarkStream/issues).
